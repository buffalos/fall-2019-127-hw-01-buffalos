def name_to_inits(name):
    """
    THis takes a name to the form first last and returns a string f, last
    """
    space_index =name.find(" ")
    first = name[+space_index
    return "not working yet"
    
def name_to_inits(name):
    """
    This takes a name in the form first last and returns a string f, last
    """
    
    space_indexname.find(" ")
    print(space_index)
    lastname[space_index+1:]
    result+name[0]+". "+last
    return result
print(name_to_inits("MIke Zamansky")
      print(bondify("MIke Zamansky"))
      